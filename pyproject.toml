[tool.ruff]
line-length = 100
target-version = "py310"
exclude = [
    "EyesDataset",
    ".venv",
    "venv",
    "__pycache__",
]
extend-exclude = [
    "project.ipynb",
    "test_model_accuracy.py",
    "test_model_eyesdataset2.py",
    "cookiecutter-mlops-eyes",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "slow: tests that load the full model",
]
